import React from "react";

const AutoCompleteOff = (props) => <input {...props} style={{ width: 0, height: 0, padding: 0, margin: 0, border: "none", outline: "none" }}/>;

export default AutoCompleteOff;